{
  "homepage": "https://ihtfy.github.io/SoundBoard/build/",
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build",
    "test": "echo \"This template does not include a test runner by default.\" && exit 1",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "@snowpack/plugin-react-refresh": "^2.3.8",
    "gh-pages": "^3.1.0",
    "snowpack": "^2.18.0"
  },
  "dependencies": {
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  }
}